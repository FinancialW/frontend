# 백엔드 주소 (constants/config.ts 에서 읽는다)
# 변경 후에는 Metro 를 재시작해야 반영된다: npx expo start
# 기본값은 배포 백엔드. 로컬 백엔드를 테스트할 때만 http://localhost:8080 등으로 잠시 변경.
# (웹 프로덕션 빌드는 Vercel 대시보드의 환경변수가 이 값보다 우선한다)
EXPO_PUBLIC_API_BASE=https://www.financialw.kro.kr
