# 백엔드 주소 (constants/config.ts 에서 읽는다)
# 변경 후에는 Metro 를 재시작해야 반영된다: npx expo start
# 시뮬레이터는 Mac과 네트워크를 공유하므로 localhost 사용.
# 실제 폰으로 테스트할 때는 Mac의 LAN IP로 변경 (예: http://172.21.96.144:8080)
EXPO_PUBLIC_API_BASE=http://localhost:8080
